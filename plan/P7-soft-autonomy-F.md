# P7 — Soft autonomy under constitutional constraints (F)

**Status:** planned  
**Depends on:** P3–P5, parent mind-drive-protocol

## Goal

Mind (or Orchestrator logic) may **propose** next acts; host retains veto; identity never auto-promotes from OSS/Chorus/satellites.

## Normative behavior

1. Emit proposal envelopes (`kind` proposal/mutation) onto blackboard + injection stack.
2. Mark clearly: not user input, not accepted state.
3. Host response required: accept / accept-with-edit / veto+reason.
4. Accept still runs through MiS gates for permanent forms.
5. Veto/outcomes → episodic buffer for future conditioning.

## Tasks

- [ ] Proposal envelope fields in schema.
- [ ] Host protocol text in session-handoff: non-optional response to high-duty and proposals.
- [ ] Forbid satellite `--save` on identity.
- [ ] Chorus/OSS remain candidate-only (parent P11/P12).

## Exit criteria

Documented proposal → inject → veto/accept path; no auto-identity commit from satellites.

## References

- Parent `(mind-drive-protocol)`, HUMAN_TOOL policy
- Thread F [2026-09-14]
